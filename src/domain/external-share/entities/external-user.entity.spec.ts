/**
 * ============================================================
 * 📦 ExternalUser 도메인 엔티티 테스트
 * ============================================================
 *
 * 🎯 테스트 대상:
 *   - ExternalUser 도메인 엔티티 클래스
 *
 * 📋 비즈니스 맥락:
 *   - 외부 파일 공유 시스템의 외부 사용자 (협력업체, 고객 등)
 *   - 관리자가 생성하며, 로그인하여 공유된 파일에 접근
 *   - 활성화/비활성화 상태로 접근 제어
 *
 * ⚠️ 중요 고려사항:
 *   - isActive가 false면 로그인 차단
 *   - 비밀번호는 해시로 저장
 *   - lastLoginAt으로 마지막 로그인 시간 추적
 * ============================================================
 */
import { ExternalUser } from './external-user.entity';

describe('ExternalUser Entity', () => {
  /**
   * 📌 테스트 시나리오: ExternalUser 엔티티 생성
   *
   * 🎯 검증 목적:
   *   ExternalUser 엔티티가 올바른 속성으로 생성되는지 확인
   *
   * ✅ 기대 결과:
   *   모든 속성이 전달된 값으로 초기화됨
   */
  it('should create an ExternalUser with all properties', () => {
    // ═══════════════════════════════════════════════════════
    // 📥 GIVEN (사전 조건 설정)
    // ═══════════════════════════════════════════════════════
    const props = {
      id: 'ext-user-123',
      username: 'partner_user',
      passwordHash: 'hashed_password_123',
      name: '홍길동',
      email: 'hong@partner.com',
      company: '협력사A',
      phone: '010-1234-5678',
      isActive: true,
      createdBy: 'admin-123',
      createdAt: new Date('2026-01-01'),
      lastLoginAt: new Date('2026-01-15'),
    };

    // ═══════════════════════════════════════════════════════
    // 🎬 WHEN (테스트 실행)
    // ═══════════════════════════════════════════════════════
    const user = new ExternalUser(props);

    // ═══════════════════════════════════════════════════════
    // ✅ THEN (결과 검증)
    // ═══════════════════════════════════════════════════════
    expect(user.id).toBe('ext-user-123');
    expect(user.username).toBe('partner_user');
    expect(user.passwordHash).toBe('hashed_password_123');
    expect(user.name).toBe('홍길동');
    expect(user.email).toBe('hong@partner.com');
    expect(user.company).toBe('협력사A');
    expect(user.phone).toBe('010-1234-5678');
    expect(user.isActive).toBe(true);
    expect(user.createdBy).toBe('admin-123');
  });

  /**
   * 📌 테스트 시나리오: 기본값으로 ExternalUser 생성
   *
   * 🎯 검증 목적:
   *   isActive가 기본값 true로 초기화되는지 확인
   *
   * ✅ 기대 결과:
   *   isActive: true (기본 활성 상태)
   */
  it('should initialize with default values (isActive = true)', () => {
    // ═══════════════════════════════════════════════════════
    // 📥 GIVEN (사전 조건 설정)
    // ═══════════════════════════════════════════════════════
    const props = {
      id: 'ext-user-123',
      username: 'partner_user',
      passwordHash: 'hashed_password_123',
      name: '홍길동',
      email: 'hong@partner.com',
      createdBy: 'admin-123',
    };

    // ═══════════════════════════════════════════════════════
    // 🎬 WHEN (테스트 실행)
    // ═══════════════════════════════════════════════════════
    const user = new ExternalUser(props);

    // ═══════════════════════════════════════════════════════
    // ✅ THEN (결과 검증)
    // ═══════════════════════════════════════════════════════
    expect(user.isActive).toBe(true);
  });

  /**
   * 📌 테스트 시나리오: 로그인 가능 여부 확인 (canLogin)
   */
  describe('canLogin', () => {
    /**
     * 🎯 검증 목적: 활성화 상태면 로그인 가능
     */
    it('should return true when isActive is true', () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const user = new ExternalUser({
        id: 'ext-user-123',
        username: 'partner_user',
        passwordHash: 'hashed_password_123',
        name: '홍길동',
        email: 'hong@partner.com',
        createdBy: 'admin-123',
        isActive: true,
      });

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN & THEN (실행 및 검증)
      // ═══════════════════════════════════════════════════════
      expect(user.canLogin()).toBe(true);
    });

    /**
     * 🎯 검증 목적: 비활성화 상태면 로그인 불가
     */
    it('should return false when isActive is false', () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const user = new ExternalUser({
        id: 'ext-user-123',
        username: 'partner_user',
        passwordHash: 'hashed_password_123',
        name: '홍길동',
        email: 'hong@partner.com',
        createdBy: 'admin-123',
        isActive: false,
      });

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN & THEN (실행 및 검증)
      // ═══════════════════════════════════════════════════════
      expect(user.canLogin()).toBe(false);
    });
  });

  /**
   * 📌 테스트 시나리오: 계정 비활성화 (deactivate)
   */
  describe('deactivate', () => {
    /**
     * 🎯 검증 목적: 활성화 상태를 비활성화로 변경
     */
    it('should set isActive to false', () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const user = new ExternalUser({
        id: 'ext-user-123',
        username: 'partner_user',
        passwordHash: 'hashed_password_123',
        name: '홍길동',
        email: 'hong@partner.com',
        createdBy: 'admin-123',
        isActive: true,
      });

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN (테스트 실행)
      // ═══════════════════════════════════════════════════════
      user.deactivate();

      // ═══════════════════════════════════════════════════════
      // ✅ THEN (결과 검증)
      // ═══════════════════════════════════════════════════════
      expect(user.isActive).toBe(false);
    });
  });

  /**
   * 📌 테스트 시나리오: 계정 활성화 (activate)
   */
  describe('activate', () => {
    /**
     * 🎯 검증 목적: 비활성화 상태를 활성화로 변경
     */
    it('should set isActive to true', () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const user = new ExternalUser({
        id: 'ext-user-123',
        username: 'partner_user',
        passwordHash: 'hashed_password_123',
        name: '홍길동',
        email: 'hong@partner.com',
        createdBy: 'admin-123',
        isActive: false,
      });

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN (테스트 실행)
      // ═══════════════════════════════════════════════════════
      user.activate();

      // ═══════════════════════════════════════════════════════
      // ✅ THEN (결과 검증)
      // ═══════════════════════════════════════════════════════
      expect(user.isActive).toBe(true);
    });
  });

  /**
   * 📌 테스트 시나리오: 마지막 로그인 시간 갱신 (updateLastLogin)
   */
  describe('updateLastLogin', () => {
    /**
     * 🎯 검증 목적: lastLoginAt을 현재 시간으로 갱신
     */
    it('should update lastLoginAt to current time', () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const user = new ExternalUser({
        id: 'ext-user-123',
        username: 'partner_user',
        passwordHash: 'hashed_password_123',
        name: '홍길동',
        email: 'hong@partner.com',
        createdBy: 'admin-123',
        lastLoginAt: new Date('2026-01-01'),
      });

      const beforeUpdate = new Date();

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN (테스트 실행)
      // ═══════════════════════════════════════════════════════
      user.updateLastLogin();

      // ═══════════════════════════════════════════════════════
      // ✅ THEN (결과 검증)
      // ═══════════════════════════════════════════════════════
      expect(user.lastLoginAt).toBeDefined();
      expect(user.lastLoginAt!.getTime()).toBeGreaterThanOrEqual(
        beforeUpdate.getTime(),
      );
    });
  });

  /**
   * 📌 테스트 시나리오: 비밀번호 변경 (updatePassword)
   */
  describe('updatePassword', () => {
    /**
     * 🎯 검증 목적: 비밀번호 해시 변경
     */
    it('should update passwordHash', () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const user = new ExternalUser({
        id: 'ext-user-123',
        username: 'partner_user',
        passwordHash: 'old_hashed_password',
        name: '홍길동',
        email: 'hong@partner.com',
        createdBy: 'admin-123',
      });

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN (테스트 실행)
      // ═══════════════════════════════════════════════════════
      user.updatePassword('new_hashed_password');

      // ═══════════════════════════════════════════════════════
      // ✅ THEN (결과 검증)
      // ═══════════════════════════════════════════════════════
      expect(user.passwordHash).toBe('new_hashed_password');
    });
  });
});
