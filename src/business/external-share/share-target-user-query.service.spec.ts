/**
 * ============================================================
 * 📦 ShareTargetUserQueryService 테스트
 * ============================================================
 *
 * 🎯 테스트 대상:
 *   - ShareTargetUserQueryService 클래스
 *
 * 📋 비즈니스 맥락:
 *   - 파일 공유 대상자 선택 UI에서 내부/외부 사용자를 통합 검색
 *   - roles.name = 'GUEST' 기반으로 내부/외부 구분
 *   - 이름, 부서, 이메일, 사용자 유형 필터링 + 페이지네이션
 *
 * ⚠️ 중요 고려사항:
 *   - 단일 SQL로 내부/외부 통합 조회 (UNION 아닌 CASE WHEN)
 *   - type 필터에 따라 WHERE 조건이 동적으로 변경됨
 *   - Role 기반 외부 사용자 판별 (r.name = 'GUEST')
 * ============================================================
 */
import { Test, TestingModule } from '@nestjs/testing';
import { DataSource } from 'typeorm';
import { ShareTargetUserQueryService } from './share-target-user-query.service';
import {
  ShareTargetUserQueryDto,
  ShareTargetUserType,
} from '../../interface/controller/share/dto/share-target-user.dto';

describe('ShareTargetUserQueryService', () => {
  let service: ShareTargetUserQueryService;
  let mockDataSource: { query: jest.Mock };

  /**
   * 🎭 Mock 설정
   * 📍 mockDataSource.query:
   *   - 실제 동작: DB에서 Raw SQL 실행
   *   - Mock 이유: 실제 DB 연결 없이 SQL 생성 로직과 결과 매핑 검증
   */
  beforeEach(async () => {
    mockDataSource = {
      query: jest.fn(),
    };

    const module: TestingModule = await Test.createTestingModule({
      providers: [
        ShareTargetUserQueryService,
        {
          provide: DataSource,
          useValue: mockDataSource,
        },
      ],
    }).compile();

    service = module.get<ShareTargetUserQueryService>(ShareTargetUserQueryService);
  });

  // ──────────────────────────────────────────────────────────
  // 필터 없이 전체 조회
  // ──────────────────────────────────────────────────────────

  describe('전체 조회 (필터 없음)', () => {
    /**
     * 📌 테스트 시나리오: 내부/외부 사용자 통합 조회
     *
     * 🎯 검증 목적:
     *   필터 없이 호출하면 내부/외부 구분 없이 모든 재직중 사용자가 반환되어야 한다.
     *   SQL의 CASE WHEN으로 type이 정확히 판별되는지 확인.
     *
     * ✅ 기대 결과:
     *   내부/외부 사용자가 함께 반환되고, 페이지네이션 정보가 정확하다.
     */
    it('내부/외부 사용자를 통합하여 반환해야 한다', async () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const query = new ShareTargetUserQueryDto();
      query.page = 1;
      query.pageSize = 20;

      mockDataSource.query
        // COUNT 쿼리
        .mockResolvedValueOnce([{ total: '2' }])
        // DATA 쿼리
        .mockResolvedValueOnce([
          {
            id: 'emp-1',
            name: '홍길동',
            email: 'hong@company.com',
            department: '개발팀',
            roleName: 'ADMIN',
            type: 'INTERNAL',
            isActive: true,
          },
          {
            id: 'emp-2',
            name: '김외부',
            email: 'kim@partner.com',
            department: '외부협력사',
            roleName: 'GUEST',
            type: 'EXTERNAL',
            isActive: true,
          },
        ]);

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN (테스트 실행)
      // ═══════════════════════════════════════════════════════
      const result = await service.findAll(query);

      // ═══════════════════════════════════════════════════════
      // ✅ THEN (결과 검증)
      // ═══════════════════════════════════════════════════════
      expect(mockDataSource.query).toHaveBeenCalledTimes(2);
      expect(result.items).toHaveLength(2);
      expect(result.totalItems).toBe(2);
      expect(result.page).toBe(1);
      expect(result.pageSize).toBe(20);

      // 내부 사용자 확인
      expect(result.items[0].id).toBe('emp-1');
      expect(result.items[0].type).toBe('INTERNAL');
      expect(result.items[0].name).toBe('홍길동');
      expect(result.items[0].roleName).toBe('ADMIN');

      // 외부 사용자 확인
      expect(result.items[1].id).toBe('emp-2');
      expect(result.items[1].type).toBe('EXTERNAL');
      expect(result.items[1].name).toBe('김외부');
      expect(result.items[1].roleName).toBe('GUEST');
    });

    /**
     * 📌 테스트 시나리오: 결과가 없을 때 빈 배열 반환
     *
     * 🎯 검증 목적:
     *   COUNT가 0이면 DATA 쿼리를 실행하지 않고 빈 결과를 반환해야 한다.
     *   불필요한 쿼리 실행 방지.
     *
     * ✅ 기대 결과:
     *   items가 빈 배열이고, totalItems가 0이다. DATA 쿼리는 호출되지 않는다.
     */
    it('결과가 없으면 빈 배열과 함께 0 totalItems를 반환해야 한다', async () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const query = new ShareTargetUserQueryDto();
      mockDataSource.query.mockResolvedValueOnce([{ total: '0' }]);

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN (테스트 실행)
      // ═══════════════════════════════════════════════════════
      const result = await service.findAll(query);

      // ═══════════════════════════════════════════════════════
      // ✅ THEN (결과 검증)
      // ═══════════════════════════════════════════════════════
      expect(result.items).toEqual([]);
      expect(result.totalItems).toBe(0);
      expect(result.totalPages).toBe(0);
      // COUNT만 호출, DATA 쿼리는 미호출
      expect(mockDataSource.query).toHaveBeenCalledTimes(1);
    });
  });

  // ──────────────────────────────────────────────────────────
  // type 필터
  // ──────────────────────────────────────────────────────────

  describe('type 필터', () => {
    /**
     * 📌 테스트 시나리오: EXTERNAL 필터 시 GUEST Role 조건 조회
     *
     * 🎯 검증 목적:
     *   type=EXTERNAL이면 WHERE절에 r.name = 'GUEST' 조건이 추가되어
     *   외부 사용자만 반환되어야 한다.
     *
     * ✅ 기대 결과:
     *   SQL에 r.name = 'GUEST' 조건이 포함된다.
     */
    it('type=EXTERNAL이면 GUEST Role 조건(r.name = \'GUEST\')을 포함해야 한다', async () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const query = new ShareTargetUserQueryDto();
      query.type = ShareTargetUserType.EXTERNAL;

      mockDataSource.query
        .mockResolvedValueOnce([{ total: '0' }]);

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN (테스트 실행)
      // ═══════════════════════════════════════════════════════
      await service.findAll(query);

      // ═══════════════════════════════════════════════════════
      // ✅ THEN (결과 검증)
      // ═══════════════════════════════════════════════════════
      const countSql = mockDataSource.query.mock.calls[0][0];
      expect(countSql).toContain("r.name = 'GUEST'");
    });

    /**
     * 📌 테스트 시나리오: INTERNAL 필터 시 GUEST Role 제외
     *
     * 🎯 검증 목적:
     *   type=INTERNAL이면 WHERE절에 GUEST Role이 아닌 조건이 추가되어
     *   내부 사용자만 반환되어야 한다.
     *
     * ✅ 기대 결과:
     *   SQL에 r.name != 'GUEST' 조건이 포함된다.
     */
    it('type=INTERNAL이면 GUEST Role 제외 조건을 포함해야 한다', async () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const query = new ShareTargetUserQueryDto();
      query.type = ShareTargetUserType.INTERNAL;

      mockDataSource.query
        .mockResolvedValueOnce([{ total: '0' }]);

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN (테스트 실행)
      // ═══════════════════════════════════════════════════════
      await service.findAll(query);

      // ═══════════════════════════════════════════════════════
      // ✅ THEN (결과 검증)
      // ═══════════════════════════════════════════════════════
      const countSql = mockDataSource.query.mock.calls[0][0];
      expect(countSql).toContain("r.name != 'GUEST'");
    });

    /**
     * 📌 테스트 시나리오: type 미지정 시 Role 필터 없음
     *
     * 🎯 검증 목적:
     *   type이 없으면 내부/외부 구분 없이 전체 조회. Role 필터가 없어야 한다.
     *
     * ✅ 기대 결과:
     *   SQL WHERE절에 r.name 관련 필터 조건이 포함되지 않는다.
     */
    it('type 미지정 시 WHERE절에 Role 필터가 없어야 한다', async () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const query = new ShareTargetUserQueryDto();

      mockDataSource.query
        .mockResolvedValueOnce([{ total: '0' }]);

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN (테스트 실행)
      // ═══════════════════════════════════════════════════════
      await service.findAll(query);

      // ═══════════════════════════════════════════════════════
      // ✅ THEN (결과 검증)
      // WHERE절만 추출하여 Role 필터 조건이 없는지 확인
      // (JOIN의 r.id 참조는 제외)
      // ═══════════════════════════════════════════════════════
      const countSql = mockDataSource.query.mock.calls[0][0];
      const whereClause = countSql.substring(countSql.indexOf('WHERE'));
      expect(whereClause).not.toContain("r.name = 'GUEST'");
      expect(whereClause).not.toContain("r.name != 'GUEST'");
    });
  });

  // ──────────────────────────────────────────────────────────
  // 검색 필터 (name, department, email)
  // ──────────────────────────────────────────────────────────

  describe('검색 필터', () => {
    /**
     * 📌 테스트 시나리오: 이름 필터 (ILIKE 부분 일치)
     *
     * 🎯 검증 목적:
     *   name 필터가 ILIKE로 적용되어 부분 일치 검색이 가능해야 한다.
     *
     * ✅ 기대 결과:
     *   SQL에 e.name ILIKE 조건이 포함되고, 파라미터에 %이름% 패턴이 전달된다.
     */
    it('name 필터가 ILIKE 부분 일치로 적용되어야 한다', async () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const query = new ShareTargetUserQueryDto();
      query.name = '홍길동';

      mockDataSource.query.mockResolvedValueOnce([{ total: '0' }]);

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN (테스트 실행)
      // ═══════════════════════════════════════════════════════
      await service.findAll(query);

      // ═══════════════════════════════════════════════════════
      // ✅ THEN (결과 검증)
      // ═══════════════════════════════════════════════════════
      const countSql = mockDataSource.query.mock.calls[0][0];
      const params = mockDataSource.query.mock.calls[0][1];
      expect(countSql).toContain('e.name ILIKE');
      expect(params).toContain('%홍길동%');
    });

    /**
     * 📌 테스트 시나리오: 부서명 필터 (ILIKE 부분 일치)
     *
     * 🎯 검증 목적:
     *   department 필터가 departments-info의 departmentName에 ILIKE로 적용되어야 한다.
     *
     * ✅ 기대 결과:
     *   SQL에 d."departmentName" ILIKE 조건이 포함된다.
     */
    it('department 필터가 ILIKE 부분 일치로 적용되어야 한다', async () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const query = new ShareTargetUserQueryDto();
      query.department = '개발';

      mockDataSource.query.mockResolvedValueOnce([{ total: '0' }]);

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN (테스트 실행)
      // ═══════════════════════════════════════════════════════
      await service.findAll(query);

      // ═══════════════════════════════════════════════════════
      // ✅ THEN (결과 검증)
      // ═══════════════════════════════════════════════════════
      const countSql = mockDataSource.query.mock.calls[0][0];
      const params = mockDataSource.query.mock.calls[0][1];
      expect(countSql).toContain('d."departmentName" ILIKE');
      expect(params).toContain('%개발%');
    });

    /**
     * 📌 테스트 시나리오: 이메일 필터 (ILIKE 부분 일치)
     *
     * 🎯 검증 목적:
     *   email 필터가 ILIKE로 적용되어야 한다.
     *
     * ✅ 기대 결과:
     *   SQL에 e.email ILIKE 조건이 포함된다.
     */
    it('email 필터가 ILIKE 부분 일치로 적용되어야 한다', async () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const query = new ShareTargetUserQueryDto();
      query.email = 'hong@';

      mockDataSource.query.mockResolvedValueOnce([{ total: '0' }]);

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN (테스트 실행)
      // ═══════════════════════════════════════════════════════
      await service.findAll(query);

      // ═══════════════════════════════════════════════════════
      // ✅ THEN (결과 검증)
      // ═══════════════════════════════════════════════════════
      const countSql = mockDataSource.query.mock.calls[0][0];
      const params = mockDataSource.query.mock.calls[0][1];
      expect(countSql).toContain('e.email ILIKE');
      expect(params).toContain('%hong@%');
    });

    /**
     * 📌 테스트 시나리오: 복합 필터 (type + name + email)
     *
     * 🎯 검증 목적:
     *   여러 필터가 동시에 적용될 때 모든 조건이 AND로 결합되어야 한다.
     *   파라미터 인덱싱이 정확해야 SQL Injection 방지와 정확한 결과를 보장한다.
     *
     * ✅ 기대 결과:
     *   모든 필터 조건이 SQL에 포함되고, 파라미터가 올바른 순서로 전달된다.
     */
    it('복합 필터가 AND 조건으로 결합되어야 한다', async () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const query = new ShareTargetUserQueryDto();
      query.type = ShareTargetUserType.INTERNAL;
      query.name = '홍';
      query.email = 'hong';

      mockDataSource.query.mockResolvedValueOnce([{ total: '0' }]);

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN (테스트 실행)
      // ═══════════════════════════════════════════════════════
      await service.findAll(query);

      // ═══════════════════════════════════════════════════════
      // ✅ THEN (결과 검증)
      // ═══════════════════════════════════════════════════════
      const countSql = mockDataSource.query.mock.calls[0][0];
      const params = mockDataSource.query.mock.calls[0][1];

      // 모든 조건 포함
      expect(countSql).toContain('e.status = \'재직중\'');
      expect(countSql).toContain("r.name != 'GUEST'");
      expect(countSql).toContain('e.name ILIKE');
      expect(countSql).toContain('e.email ILIKE');

      // 파라미터 순서: %홍%, %hong% (EXTERNAL_DEPARTMENT_ID 파라미터 제거됨)
      expect(params[0]).toBe('%홍%');
      expect(params[1]).toBe('%hong%');
    });
  });

  // ──────────────────────────────────────────────────────────
  // 페이지네이션
  // ──────────────────────────────────────────────────────────

  describe('페이지네이션', () => {
    /**
     * 📌 테스트 시나리오: LIMIT/OFFSET이 올바르게 적용
     *
     * 🎯 검증 목적:
     *   page=2, pageSize=10이면 LIMIT 10 OFFSET 10으로 변환되어야 한다.
     *
     * ✅ 기대 결과:
     *   DATA 쿼리의 마지막 두 파라미터가 pageSize와 offset이다.
     */
    it('LIMIT/OFFSET 파라미터가 정확히 전달되어야 한다', async () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const query = new ShareTargetUserQueryDto();
      query.page = 2;
      query.pageSize = 10;

      mockDataSource.query
        .mockResolvedValueOnce([{ total: '25' }])
        .mockResolvedValueOnce([]);

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN (테스트 실행)
      // ═══════════════════════════════════════════════════════
      await service.findAll(query);

      // ═══════════════════════════════════════════════════════
      // ✅ THEN (결과 검증)
      // ═══════════════════════════════════════════════════════
      const dataParams = mockDataSource.query.mock.calls[1][1];
      // 마지막 두 파라미터: pageSize=10, offset=(2-1)*10=10
      expect(dataParams[dataParams.length - 2]).toBe(10);
      expect(dataParams[dataParams.length - 1]).toBe(10);
    });

    /**
     * 📌 테스트 시나리오: 페이지네이션 메타 정보 정확성
     *
     * 🎯 검증 목적:
     *   totalPages, hasNext, hasPrev가 totalItems와 page/pageSize로 정확히 계산되어야 한다.
     *
     * ✅ 기대 결과:
     *   25건, pageSize=10, page=2이면 totalPages=3, hasNext=true, hasPrev=true
     */
    it('페이지네이션 메타 정보가 정확해야 한다', async () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const query = new ShareTargetUserQueryDto();
      query.page = 2;
      query.pageSize = 10;

      mockDataSource.query
        .mockResolvedValueOnce([{ total: '25' }])
        .mockResolvedValueOnce([
          { id: 'emp-1', name: '테스트', email: 'test@t.com', department: '팀', roleName: null, type: 'INTERNAL', isActive: true },
        ]);

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN (테스트 실행)
      // ═══════════════════════════════════════════════════════
      const result = await service.findAll(query);

      // ═══════════════════════════════════════════════════════
      // ✅ THEN (결과 검증)
      // ═══════════════════════════════════════════════════════
      expect(result.totalItems).toBe(25);
      expect(result.totalPages).toBe(3); // ceil(25/10)
      expect(result.hasNext).toBe(true); // page 2 < totalPages 3
      expect(result.hasPrev).toBe(true); // page 2 > 1
    });
  });

  // ──────────────────────────────────────────────────────────
  // 결과 매핑
  // ──────────────────────────────────────────────────────────

  describe('결과 매핑', () => {
    /**
     * 📌 테스트 시나리오: roleName이 null인 경우 null 반환
     *
     * 🎯 검증 목적:
     *   Role이 미부여된 사용자의 roleName이 undefined가 아니라 명시적 null이어야 한다.
     *   프론트에서 null 체크로 역할 미부여를 표시하기 위함.
     *
     * ✅ 기대 결과:
     *   roleName이 null이다.
     */
    it('Role 미부여 사용자의 roleName은 null이어야 한다', async () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const query = new ShareTargetUserQueryDto();

      mockDataSource.query
        .mockResolvedValueOnce([{ total: '1' }])
        .mockResolvedValueOnce([
          {
            id: 'emp-1',
            name: '홍길동',
            email: 'hong@test.com',
            department: '개발팀',
            roleName: undefined, // DB에서 NULL → JS undefined
            type: 'INTERNAL',
            isActive: true,
          },
        ]);

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN (테스트 실행)
      // ═══════════════════════════════════════════════════════
      const result = await service.findAll(query);

      // ═══════════════════════════════════════════════════════
      // ✅ THEN (결과 검증)
      // ═══════════════════════════════════════════════════════
      expect(result.items[0].roleName).toBeNull();
    });

    /**
     * 📌 테스트 시나리오: 재직중 조건이 항상 SQL에 포함
     *
     * 🎯 검증 목적:
     *   퇴사자가 공유 대상자로 노출되면 안 되므로, 재직중 조건은 항상 포함되어야 한다.
     *
     * ✅ 기대 결과:
     *   SQL WHERE절에 e.status = '재직중' 조건이 항상 포함된다.
     */
    it('재직중 조건이 항상 SQL에 포함되어야 한다', async () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const query = new ShareTargetUserQueryDto();

      mockDataSource.query.mockResolvedValueOnce([{ total: '0' }]);

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN (테스트 실행)
      // ═══════════════════════════════════════════════════════
      await service.findAll(query);

      // ═══════════════════════════════════════════════════════
      // ✅ THEN (결과 검증)
      // ═══════════════════════════════════════════════════════
      const countSql = mockDataSource.query.mock.calls[0][0];
      expect(countSql).toContain("e.status = '재직중'");
    });

    /**
     * 📌 테스트 시나리오: SQL에 roles JOIN이 포함
     *
     * 🎯 검증 목적:
     *   외부/내부 사용자 구분을 위해 roles 테이블 JOIN이 반드시 포함되어야 한다.
     *
     * ✅ 기대 결과:
     *   SQL에 roles 테이블 JOIN이 포함된다.
     */
    it('SQL에 roles 테이블 JOIN이 포함되어야 한다', async () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const query = new ShareTargetUserQueryDto();

      mockDataSource.query.mockResolvedValueOnce([{ total: '0' }]);

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN (테스트 실행)
      // ═══════════════════════════════════════════════════════
      await service.findAll(query);

      // ═══════════════════════════════════════════════════════
      // ✅ THEN (결과 검증)
      // ═══════════════════════════════════════════════════════
      const countSql = mockDataSource.query.mock.calls[0][0];
      expect(countSql).toContain('LEFT JOIN roles r ON u.role_id = r.id');
    });
  });
});
