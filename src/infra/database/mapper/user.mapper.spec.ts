/**
 * ============================================================
 * 📦 User Mapper 테스트
 * ============================================================
 *
 * 🎯 테스트 대상:
 *   - UserMapper 클래스
 *
 * 📋 비즈니스 맥락:
 *   - ORM 엔티티와 도메인 엔티티 간의 변환 담당
 *   - 영속성 레이어와 도메인 레이어 분리를 위한 중요한 역할
 *
 * ⚠️ 중요 고려사항:
 *   - 변환 시 데이터 손실이 없어야 함
 *   - null 값 처리 주의
 * ============================================================
 */
import { UserMapper } from './user.mapper';
import { User } from '../../../domain/user/entities/user.entity';
import { UserOrmEntity } from '../entities/user.orm-entity';

describe('UserMapper', () => {
  /**
   * 📌 테스트 시나리오: ORM → Domain 변환
   *
   * 🎯 검증 목적:
   *   DB에서 조회한 ORM 엔티티를 도메인 엔티티로 변환
   *
   * ✅ 기대 결과:
   *   모든 속성이 올바르게 매핑됨
   */
  describe('toDomain', () => {
    it('should convert ORM entity to Domain entity', () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const ormEntity: UserOrmEntity = {
        id: 'user-123',
        roleId: 'role-456',
        isActive: true,
        createdAt: new Date('2026-01-01'),
        updatedAt: new Date('2026-01-15'),
      };

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN (테스트 실행)
      // ═══════════════════════════════════════════════════════
      const domainEntity = UserMapper.toDomain(ormEntity);

      // ═══════════════════════════════════════════════════════
      // ✅ THEN (결과 검증)
      // ═══════════════════════════════════════════════════════
      expect(domainEntity).toBeInstanceOf(User);
      expect(domainEntity.id).toBe('user-123');
      expect(domainEntity.roleId).toBe('role-456');
      expect(domainEntity.isActive).toBe(true);
      expect(domainEntity.createdAt).toEqual(new Date('2026-01-01'));
      expect(domainEntity.updatedAt).toEqual(new Date('2026-01-15'));
    });

    it('should handle null roleId', () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const ormEntity: UserOrmEntity = {
        id: 'user-123',
        roleId: null,
        isActive: true,
        createdAt: new Date(),
        updatedAt: new Date(),
      };

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN (테스트 실행)
      // ═══════════════════════════════════════════════════════
      const domainEntity = UserMapper.toDomain(ormEntity);

      // ═══════════════════════════════════════════════════════
      // ✅ THEN (결과 검증)
      // ═══════════════════════════════════════════════════════
      expect(domainEntity.roleId).toBeNull();
    });
  });

  /**
   * 📌 테스트 시나리오: Domain → ORM 변환
   *
   * 🎯 검증 목적:
   *   도메인 엔티티를 DB 저장용 ORM 엔티티로 변환
   *
   * ✅ 기대 결과:
   *   모든 속성이 올바르게 매핑됨
   */
  describe('toOrm', () => {
    it('should convert Domain entity to ORM entity', () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const domainEntity = new User({
        id: 'user-123',
        roleId: 'role-456',
        isActive: true,
        createdAt: new Date('2026-01-01'),
        updatedAt: new Date('2026-01-15'),
      });

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN (테스트 실행)
      // ═══════════════════════════════════════════════════════
      const ormEntity = UserMapper.toOrm(domainEntity);

      // ═══════════════════════════════════════════════════════
      // ✅ THEN (결과 검증)
      // ═══════════════════════════════════════════════════════
      expect(ormEntity.id).toBe('user-123');
      expect(ormEntity.roleId).toBe('role-456');
      expect(ormEntity.isActive).toBe(true);
      expect(ormEntity.createdAt).toEqual(new Date('2026-01-01'));
      expect(ormEntity.updatedAt).toEqual(new Date('2026-01-15'));
    });

    it('should handle null roleId', () => {
      // ═══════════════════════════════════════════════════════
      // 📥 GIVEN (사전 조건 설정)
      // ═══════════════════════════════════════════════════════
      const domainEntity = new User({
        id: 'user-123',
        roleId: null,
        isActive: true,
        createdAt: new Date(),
        updatedAt: new Date(),
      });

      // ═══════════════════════════════════════════════════════
      // 🎬 WHEN (테스트 실행)
      // ═══════════════════════════════════════════════════════
      const ormEntity = UserMapper.toOrm(domainEntity);

      // ═══════════════════════════════════════════════════════
      // ✅ THEN (결과 검증)
      // ═══════════════════════════════════════════════════════
      expect(ormEntity.roleId).toBeNull();
    });
  });
});
